// compile_fail: imported private struct fields cannot be assigned

import cross_module_test.types;

fn test_private_field_assignment_contract() -> int {
    let mut box = types.PrivateBox(1);
    box.value = 2;
    box.get()
}
