// Test for generic box<T> syntax
struct test(
    pub expected_type: string,
    pub expected_value: string,
);

struct Point(
    x: int,
    y: int,
);

// Test basic box<T> syntax parsing
@test(expected_type = "int", expected_value = "42")
fn test_box_syntax() -> int {
    // For now, we just test that the syntax parses
    // Full box semantics will be implemented later
    let x = 42;
    x
}
