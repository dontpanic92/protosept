struct test(
    pub expected_type: string,
    pub expected_value: string,
);

enum MyError {
    Error1,
}

fn helper() -> int {
    42
}

fn risky_no_return() throws {
    let x = helper();
}

@test(expected_type = "int", expected_value = "42")
fn test_throws_no_return() -> int {
    42
}
