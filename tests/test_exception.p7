/*
[testcase]
entrypoint = "test_exception"
expected_type = "int"
expected_value = "42"
*/

enum MyError {
    SomeError,
}

fn may_throw(value: int) -> int throws {
    if value == 0 {
        throw MyError.SomeError;
    }
    value
}

fn test_exception() -> int {
    let result = try may_throw(0) else 42;
    result
}
