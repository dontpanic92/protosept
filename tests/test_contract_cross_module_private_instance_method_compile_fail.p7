// compile_fail: imported private instance methods cannot be called

import cross_module_test.types;

fn test_private_instance_method_contract() -> int {
    let box = types.PrivateBox(1);
    box.secret()
}
