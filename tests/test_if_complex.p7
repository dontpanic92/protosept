struct test(
    pub expected_type: string,
    pub expected_value: string,
);

@test(expected_type = "int", expected_value = "1")
fn test_if_with_bool_logic() -> int {
    let x = 10;
    let cond = x > 5;
    
    let result = if cond {
        1
    } else {
        0
    };
    
    result
}
