// compile_fail: use-after-move

// This test should fail to compile - use after move
struct Point(
  pub x: int,
  pub y: int,
);

fn test_use_after_move() -> int {
  let p = Point(x = 1, y = 2);
  let q = p;  // Move p into q
  return p.x;  // ERROR: Use of moved value 'p'
}
