// compile_fail: struct missing required protocol method
// Test struct protocol conformance - missing method
import test.test;

// Define a protocol
proto Printable {
  fn print(self: ref<Printable>) -> unit;
  fn display(self: ref<Printable>) -> string;
}

// Struct claiming to conform but missing display method
struct[Printable] Point(
  x: int,
  y: int,
) {
  pub fn print(self: ref<Point>) -> unit {
    // Only implements print, missing display
  }
}

