// compile_fail: imported private struct fields cannot be updated by name

import cross_module_test.types;

fn test_private_field_update_contract() -> int {
    let box = types.PrivateBox(1);
    let updated = types.PrivateBox(...box, value = 2);
    updated.get()
}
