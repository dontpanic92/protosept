// Test import with alias
struct test(
    pub expected_type: string,
    pub expected_value: string,
);

import std.collections.list as MyList;

@test(expected_type = "int", expected_value = "100")
fn test_import_alias() -> int {
    return 100;
}
