// Test type field access
import test.test;

type UserId(int) {
    pub fn new(id: int) -> UserId {
        return Self(id);
    }
}

@test(expected_type = "int", expected_value = "100")
fn test_type_field() -> int {
    let uid = UserId.new(100);
    uid.0
}
