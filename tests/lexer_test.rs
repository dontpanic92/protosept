
#[cfg(test)]
mod tests {
    use std::fs::File;
    use std::io::{self, Read};
    use std::path::Path;
    use p7lang::lexer::{Lexer, Token};
    
    fn read_file_to_string(filepath: &Path) -> io::Result<String> {
        let mut file = File::open(filepath)?;
        let mut contents = String::new();
        file.read_to_string(&mut contents)?;
        Ok(contents)
    }

    #[test]
    fn test_lexer() {
        let filepath = Path::new("tests/test.p7");
        let input = read_file_to_string(filepath).expect("Failed to read test file");
        let mut lexer = Lexer::new(input);

        let expected_tokens = vec![
            Token::Enum,
            Token::Identifier("SomeErrors".to_string()),
            Token::OpenBrace,
            Token::Identifier("NumberIsNot42".to_string()),
            Token::Comma,
            Token::CloseBrace,
            Token::Fn,
            Token::Identifier("add".to_string()),
            Token::OpenParen,
            Token::Identifier("a".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::Comma,
            Token::Identifier("b".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("number".to_string()),
            Token::OpenBrace,
            Token::Identifier("a".to_string()),
            Token::Plus,
            Token::Identifier("b".to_string()),
            Token::CloseBrace,
            Token::Fn,
            Token::Identifier("sub".to_string()),
            Token::OpenParen,
            Token::Identifier("a".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::Comma,
            Token::Identifier("b".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("number".to_string()),
            Token::OpenBrace,
            Token::Return,
            Token::Identifier("a".to_string()),
            Token::Minus,
            Token::Identifier("b".to_string()),
            Token::Semicolon,
            Token::CloseBrace,
            Token::Fn,
            Token::OpenBracket,
            Token::Identifier("throws".to_string()),
            Token::CloseBracket,
            Token::Identifier("test42".to_string()),
            Token::OpenParen,
            Token::Identifier("a".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("number".to_string()),
            Token::OpenBrace,
            Token::If,
            Token::Identifier("a".to_string()),
            Token::NotEquals,
            Token::Integer(42),
            Token::OpenBrace,
            Token::Throw,
            Token::Identifier("NumberIsNot42".to_string()),
            Token::CloseBrace,
            Token::Identifier("a".to_string()),
            Token::CloseBrace,
            Token::Fn,
            Token::Identifier("assign42".to_string()),
            Token::OpenParen,
            Token::Identifier("a".to_string()),
            Token::Colon,
            Token::Ampersand,
            Token::Identifier("number".to_string()),
            Token::CloseParen,
            Token::OpenBrace,
            Token::Identifier("a".to_string()),
            Token::Assignment,
            Token::Integer(42),
            Token::Semicolon,
            Token::CloseBrace,
            Token::Struct,
            Token::Identifier("Vec2".to_string()),
            Token::OpenBrace,
            Token::Identifier("secret".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::Assignment,
            Token::Integer(42),
            Token::Semicolon,
            Token::Pub,
            Token::Identifier("x".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::Assignment,
            Token::Integer(0),
            Token::Semicolon,
            Token::Pub,
            Token::Identifier("y".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::Assignment,
            Token::Integer(0),
            Token::Semicolon,
            Token::Pub,
            Token::Fn,
            Token::Identifier("new".to_string()),
            Token::OpenParen,
            Token::Identifier("x".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::Comma,
            Token::Identifier("y".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("Self".to_string()),
            Token::OpenBrace,
            Token::Identifier("Self".to_string()),
            Token::OpenBrace,
            Token::Identifier("x".to_string()),
            Token::Comma,
            Token::Identifier("y".to_string()),
            Token::CloseBrace,
            Token::CloseBrace,
            Token::Pub,
            Token::Fn,
            Token::Identifier("add".to_string()),
            Token::OpenParen,
            Token::Identifier("self".to_string()),
            Token::Comma,
            Token::Identifier("other".to_string()),
            Token::Colon,
            Token::Identifier("Self".to_string()),
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("Self".to_string()),
            Token::OpenBrace,
            Token::Identifier("Self".to_string()),
            Token::OpenBrace,
            Token::Identifier("x".to_string()),
            Token::Colon,
            Token::Identifier("self".to_string()),
            Token::Dot,
            Token::Identifier("x".to_string()),
            Token::Plus,
            Token::Identifier("other".to_string()),
            Token::Dot,
            Token::Identifier("x".to_string()),
            Token::Comma,
            Token::Identifier("y".to_string()),
            Token::Colon,
            Token::Identifier("self".to_string()),
            Token::Dot,
            Token::Identifier("y".to_string()),
            Token::Plus,
            Token::Identifier("other".to_string()),
            Token::Dot,
            Token::Identifier("y".to_string()),
            Token::Comma,
            Token::CloseBrace,
            Token::CloseBrace,
            Token::Pub,
            Token::Fn,
            Token::Identifier("get_secret".to_string()),
            Token::OpenParen,
            Token::Identifier("self".to_string()),
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("number".to_string()),
            Token::OpenBrace,
            Token::Identifier("self".to_string()),
            Token::Dot,
            Token::Identifier("secret".to_string()),
            Token::CloseBrace,
            Token::Pub,
            Token::Fn,
            Token::Identifier("set_secret".to_string()),
            Token::OpenParen,
            Token::Ampersand,
            Token::Identifier("self".to_string()),
            Token::Comma,
            Token::Identifier("value".to_string()),
            Token::Colon,
            Token::Identifier("number".to_string()),
            Token::CloseParen,
            Token::OpenBrace,
            Token::Identifier("self".to_string()),
            Token::Dot,
            Token::Identifier("secret".to_string()),
            Token::Assignment,
            Token::Identifier("value".to_string()),
            Token::Semicolon,
            Token::CloseBrace,
            Token::CloseBrace,
            Token::Fn,
            Token::Identifier("test".to_string()),
            Token::OpenParen,
            Token::CloseParen,
            Token::RightArrow,
            Token::Identifier("number".to_string()),
            Token::OpenBrace,
            Token::Let,
            Token::Identifier("a".to_string()),
            Token::Assignment,
            Token::Identifier("Vec2".to_string()),
            Token::Dot,
            Token::Identifier("new".to_string()),
            Token::OpenParen,
            Token::Integer(1),
            Token::Comma,
            Token::Float(2.5),
            Token::CloseParen,
            Token::Semicolon,
            Token::Let,
            Token::Identifier("b".to_string()),
            Token::Assignment,
            Token::Identifier("Vec2".to_string()),
            Token::Dot,
            Token::Identifier("new".to_string()),
            Token::OpenParen,
            Token::Integer(3),
            Token::Comma,
            Token::Float(4.5),
            Token::CloseParen,
            Token::Semicolon,
            Token::Let,
            Token::Identifier("c".to_string()),
            Token::Assignment,
            Token::Identifier("a".to_string()),
            Token::Dot,
            Token::Identifier("add".to_string()),
            Token::OpenParen,
            Token::Identifier("b".to_string()),
            Token::CloseParen,
            Token::Semicolon,
            Token::Let,
            Token::Identifier("d".to_string()),
            Token::Assignment,
            Token::Identifier("sub".to_string()),
            Token::OpenParen,
            Token::Identifier("a".to_string()),
            Token::Dot,
            Token::Identifier("x".to_string()),
            Token::Comma,
            Token::Identifier("b".to_string()),
            Token::Dot,
            Token::Identifier("y".to_string()),
            Token::CloseParen,
            Token::Semicolon,
            Token::Let,
            Token::Identifier("e".to_string()),
            Token::Assignment,
            Token::Try,
            Token::Identifier("test42".to_string()),
            Token::OpenParen,
            Token::Identifier("d".to_string()),
            Token::CloseParen,
            Token::Else,
            Token::OpenBrace,
            Token::Identifier("err".to_string()),
            Token::Colon,
            Token::Identifier("SomeErrors".to_string()),
            Token::Dot,
            Token::Identifier("NumberIsNot42".to_string()),
            Token::FatRightArrow,
            Token::Integer(0),
            Token::Comma,
            Token::CloseBrace,
            Token::Let,
            Token::Identifier("f".to_string()),
            Token::Assignment,
            Token::Try,
            Token::Identifier("test42".to_string()),
            Token::OpenParen,
            Token::Identifier("e".to_string()),
            Token::CloseParen,
            Token::Else,
            Token::Integer(0),
            Token::Semicolon,
            Token::Identifier("assign42".to_string()),
            Token::OpenParen,
            Token::Ampersand,
            Token::Identifier("f".to_string()),
            Token::CloseParen,
            Token::Semicolon,
            Token::Identifier("f".to_string()),
            Token::CloseBrace
        ];

        for (i, expected_token) in expected_tokens.iter().enumerate() {
            let actual_token = lexer.next_token();
            assert_eq!(actual_token, *expected_token, "error while proccessing token #{}", i);
            println!("{:?}", actual_token);
        }
    }
}