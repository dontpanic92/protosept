// compile_fail: imported private top-level types cannot be named or constructed

import cross_module_test.types;

fn test_private_type_contract() -> int {
    let hidden = types.HiddenStruct(1);
    hidden.value
}
