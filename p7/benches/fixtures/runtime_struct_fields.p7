struct Point(
    x: int,
    y: int,
);

fn main() -> int {
    let mut total = 0;
    let mut i = 0;
    loop {
        if i > 500 {
            break;
        }
        let p = Point(x = i, y = i + 1);
        total = total + p.x + p.y;
        i = i + 1;
    }
    total
}
